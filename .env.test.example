# Test Environment Configuration
NODE_ENV=test

# Test Database (use a separate test database)
TEST_DATABASE_URL=postgresql://username:password@localhost:5432/arcanum_scribe_test

# Test Redis (optional, can use a separate Redis instance)
TEST_REDIS_URL=redis://localhost:6379/1

# Test API Keys (use test/mock keys)
OPENROUTER_API_KEY=test-openrouter-key
ANTHROPIC_API_KEY=test-anthropic-key
OPENAI_API_KEY=test-openai-key
GOOGLE_GENERATIVE_AI_API_KEY=test-google-key
FAL_API_KEY=test-fal-key

# Test Stripe (use test keys)
STRIPE_SECRET_KEY=sk_test_your_test_key_here
STRIPE_WEBHOOK_SECRET=whsec_test_webhook_secret

# Test Frontend URL
FRONTEND_URL=http://localhost:3000

# Test CDN Configuration
CDN_PROVIDER=local
CDN_BASE_URL=http://localhost:3000/uploads
CDN_UPLOAD_PATH=./test-uploads

# Disable external services in tests
DISABLE_EMAIL_NOTIFICATIONS=true
DISABLE_ANALYTICS=true

# Test JWT Secret
JWT_SECRET=test-jwt-secret-key